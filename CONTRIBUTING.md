Follow these steps:
1. Create a branch: git checkout -b <yourname>/<short-task>
2. Run linters & tests locally: pre-commit run --all-files; pytest
3. Open a PR to `main` with clear description and reviewers.
